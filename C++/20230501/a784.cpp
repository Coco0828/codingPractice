#include <iostream>
#include <string>
using namespace std;

int main(){
	string input;
	while(getline(cin, input) && input != "END"){
		int k = 0;
		char *ptr;
		char a, b;
		ptr = strtok(input, "-");
		while(ptr != NULL){
			if(k == 0){
				a = ptr
			}
		}
		
	}
	
	return 0;
}
